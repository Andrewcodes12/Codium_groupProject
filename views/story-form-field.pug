
include utils.pug

input(type='hidden' name='_csrf' value=csrfToken)
+field('Title', 'title', story.title, class ="title")
+field('subtitle', 'subtitle', story.subtitle, class ="subtitle")
+field('body', 'body', story.body, class ="body")
label(for="topic", class ="topic") Topic
